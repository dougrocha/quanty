query GetGuildConfig($guildId: String!) {
  guildConfig(guildId: $guildId) {
    id
    prefix
    premium
    guildSettings {
      defaultJoinRole
      djRole
      globalCooldown
      nsfw
      musicTimeOut
      id
    }
    guildPlugins {
      anime
      autoMod
      id
    }
  }
}
