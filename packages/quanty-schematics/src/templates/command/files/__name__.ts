import { Command } from '@quanty/framework'

export const command: Command = {
  name: `<%= name %>`,
  description: '<%= description %>',
  category: '<%= category %>',
  cmdType: '<%= cmdType %>',
  run: async () => {},
}
